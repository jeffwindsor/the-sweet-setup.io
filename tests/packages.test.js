// const { packageResolver } = require('../src/resolvers/packageResolver')

// test("Adding 1 + 1 equals 2", () => {
//   const p = { type: 'Arch', id: 'test-package'}
//   const actual = packageResolver(p);
//   expect(actual).toBe("wrong")
// })